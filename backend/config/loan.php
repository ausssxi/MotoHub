<?php

return [
    // 実質年率（例）。ディーラー・信販系バイクローンの一般水準 年3〜6% の中位を想定。
    // ※JS計算機(loan-simulator.js)の既定金利と一致させること（食い違うとユーザーが混乱するため）。
    'sample_apr' => 4.9,

    // 支払回数（静的ブロックに出す3本）。
    'terms' => [24, 36, 60],

    // この価格（円）未満は不自然な少額行を出さないためブロックごと非表示。
    'min_price' => 50000,

    // 表示の丸め単位（円）。1 = ¥1単位（JSの Math.floor と完全一致）。
    'round_unit' => 1,

    // 金利の出典メモ・最終確認（保険ブロックと同じ作法で小さく表示）。
    'source_note' => 'ディーラー・信販系バイクローンの一般的な金利水準（年3〜6%）を想定',
    'checked_at' => '2026-07',
];
