<?php

declare(strict_types=1);

return [
    /*
    |--------------------------------------------------------------------------
    | ニュースソースとして扱わない source（部分一致・大文字小文字を問わない）
    |--------------------------------------------------------------------------
    |
    | Google News 経由で入ってくるが、実態は中古車検索・在庫・店舗ページで
    | 記事メディアではない source。ここに載せた文字列を source が部分一致で
    | 含む場合、news:fetch は取り込まず、news:purge-junk は削除対象にする。
    |
    | ★ 例外: タイトルに「試乗レポート」を含むものは記事として成立するので残す
    |    （判定は NewsJunkFilter が行う）。
    |
    */
    'excluded_sources' => [
        'GooBike',
        'goobike.com',
        'グーバイク',
        // 自転車専門メディア（MotoHub のニュースとしては場違い）。
        'サイクルスポーツ.jp',
        'cyclowired.jp',
    ],
];
