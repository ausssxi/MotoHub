<?php

/*
|--------------------------------------------------------------------------
| 新基準原付ハブ（/shinkijun-gentsuki）のデータ定義
|--------------------------------------------------------------------------
| モデルは「正確な name」で解決（slug は seo_url がモデル側で持つ）。
| target = 2025新基準原付モデル本体（新古車/低走行が実在庫・title に「新基準原付」実記載を確認済み）。
| base   = 旧仕様の110cc(原付二種)中古＝タマ豊富。old50 = 旧50cc中古（比較用）。
| いずれも実在庫のみを集計し、無い項目は断定しない。
*/
return [
    'slug' => 'shinkijun-gentsuki',

    'target_models' => [
        'スーパーカブ110 lite',
        'クロスカブ110 lite',
        'dio110 lite',
        'jog one',
    ],

    'base_models' => [
        'スーパーカブ110',
        'クロスカブ110',
        'dio110',
        'ジョグ125',
    ],

    'old50_models' => [
        'スーパーカブ50',
        'jog',
    ],
];
