<?php

return [
    /*
    |--------------------------------------------------------------------------
    | UGC 統合スレッド（車種ページ）の段階的開放フラグ
    |--------------------------------------------------------------------------
    | thread_create_open : スレッド（質問/相談）の新規作成を許可するか。
    | replies_open       : 一般ユーザーの返信投稿を許可するか（false=公式必答のみ閲覧）。
    | Phase1 は「作成可＋必答ON＋返信可」で開始し、engagement を見て運用で調整する。
    | 返信・スレは即反映＋通報キル（既存UGCと同じ）で、承認制にしたい場合は運用でstatus管理。
    */
    'thread_create_open' => env('UGC_THREAD_CREATE_OPEN', true),
    'replies_open' => env('UGC_REPLIES_OPEN', true),
];
