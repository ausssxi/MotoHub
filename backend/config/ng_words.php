<?php

declare(strict_types=1);

/*
|--------------------------------------------------------------------------
| NGワード（投稿の入口フィルタ）
|--------------------------------------------------------------------------
| ショップ口コミ等のUGC投稿を保存前に検査する語リスト。運営がこのファイルを
| 編集して増減できる（変更後は config:cache）。
|
| ⚠️ 方針: 「明白に不適切」なものだけを入れる。正当な批判（対応が悪い/遅い 等）は
| 許容する（ネガティブ投稿は即反映する方針のため、批判＝NGにしない）。日本語の
| 部分一致は誤爆しやすいので、一般語・地名に含まれる文字列は避ける。運用しながら
| 少しずつ追加する。初期リストは最小限。
|
| words: 部分一致（大文字小文字は無視）。1つでも含めば投稿を弾く。
*/

return [
    'words' => [
        // 強い侮蔑・差別語（明白に不適切なもののみ・最小限）
        '死ね',
        '殺す',
        'ぶっ殺',
        'キチガイ',
        'きちがい',
        // スパム・違法勧誘の定番
        '副業で稼',
        '簡単に稼げ',
        '出会い系',
        '援助交際',
    ],
];
