<?php

declare(strict_types=1);

return [
    /*
    |--------------------------------------------------------------------------
    | ルート周辺GSの「少ない/多い」しきい値
    |--------------------------------------------------------------------------
    |
    | 周辺のガソリンスタンドがこの件数「未満」なら、件数を主役にしてリストも出す
    | （山間ルート＝給油計画が価値になる）。この件数「以上」なら件数だけ述べ、リストは
    | 出さず地図リンクに委ねる（幹線沿い＝どこにでもあり薄くなる）。
    | ★煽らない・事実だけ（config/diagnosis.php の方針と揃える）。
    |
    */
    'gas_sparse_threshold' => 40,
];
