<?php

return [
    /*
    |--------------------------------------------------------------------------
    | パーツカテゴリ実勢価格の集計パラメータ
    |--------------------------------------------------------------------------
    | parts:compute-category-prices（→ parts_category_price_stats）で使う。
    | 検索キーワードは config/parts-categories.php の keywords[] を使う（ここには置かない）。
    */

    // 重複除去後の取得件数がこれ未満のカテゴリは、統計として保存しない（四分位が不安定なため）。
    'min_products' => 20,

    // 1キーワードあたりの取得件数。ProductSearchService 側で最大30に丸められる。
    // keywords[] の全要素で検索して統合するため、実際のサンプルはこれ×キーワード数（重複除去前）。
    'hits' => 30,
];
