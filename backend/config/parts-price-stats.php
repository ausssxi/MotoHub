<?php

return [
    /*
    |--------------------------------------------------------------------------
    | パーツカテゴリ実勢価格の集計パラメータ
    |--------------------------------------------------------------------------
    | parts:compute-category-prices（→ parts_category_price_stats）で使う。
    | 検索キーワードは config/parts-categories.php の keywords[] を使う（ここには置かない）。
    */

    // 重複除去後の取得件数がこれ未満のカテゴリは、統計として保存しない（四分位が不安定なため）。
    'min_products' => 20,

    // 1キーワードあたりの取得件数。ProductSearchService 側で最大30に丸められる。
    // keywords[] の全要素で検索して統合するため、実際のサンプルはこれ×キーワード数（重複除去前）。
    'hits' => 30,

    // パーツカテゴリページに表示する商品カードの保存件数（parts_category_products）。
    'top_products' => 12,

    // 商品名にこれらのいずれかを含む商品を、価格統計・商品カードの「両方」から除外する（重複除去より前・大小/全半角無視）。
    // 主たる対策は parts-categories.php の search_keywords。ここは補助（明らかにバイク用品でないものだけ）。増やして解決しないこと。
    'exclude_title_contains' => [
        'ストール', 'スカーフ', 'バンダナ',
        'チェーンソー',
        '空調服', 'ファン付き',
        'サマータイヤ', 'スタッドレス',
    ],
];
